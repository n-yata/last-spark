# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### タスクスキップが許可される唯一のケース
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

---

## フェーズ1: 企画ブリーフの確定（STEP1-5）

- [x] worktree（feature/story-redesign）作成・steering 起動
- [x] requirements.md 作成（要求・背景・受け入れ条件）
- [x] design.md 作成（テーマ／ログライン／キャラ／世界観／プロット骨子＝企画ブリーフ）
- [x] tasklist.md 作成（本ファイル）
- [x] シャビの承認を得る（ブリーフ確定）

## フェーズ2: 完成稿執筆（単独執筆へ方針変更）

> シャビ判断で「脚本チーム競作」→「単独執筆」へ変更。競作タスクは不要のため下記に置換。

- [x] ~~脚本チーム競作（STEP6-7）~~（方針変更により不要: シャビ判断で単独執筆に変更）
- [x] 発火モデルをコード調査で確定（intro/stageStart/firstEnemyDefeated/terraFound/eclipseVoice/eclipseReaction/bossDefeated）
- [x] 管理AIの具体名を確定（「管理者」＝シャビ確定）
- [x] 完成稿ドラフトを単独執筆 → `draft-story.md`（前任分が消失していたため再作成）
- [x] 旧版チェックリスト（design.md「🚑」）6項目の解消を確認
- [x] シャビへ提出・承認（4論点すべて GO）

## フェーズ3: 正本への反映

- [x] `docs/story.md`（北極星）を承認稿で更新（テーマ／管理者命名／転／伏線／エンディング修正）
- [x] `src/config/story/`（stage1-6 + cutscenes）の実テキストを差し替え（terraFound を stage2/5 に新設）
- [x] `src/types/story.ts` の種別と齟齬がないか確認（型変更不要）
- [x] `docs/story.md` と `src/config/story/` の一致を確認
- [x] コミット前にクルトワ（security-engineer）のセキュリティレビュー（指摘なし）

## フェーズ4: 品質チェック

- [x] リントエラーがないことを確認（`npm run lint`）
- [x] 型エラーがないことを確認（`npm run typecheck`）
- [x] テストが通ることを確認（`npm test` 489 passed。storyData/cutscenes の期待値を新確定版へ更新）
- [x] ビルドが成功することを確認（`npm run build`）

## フェーズ5: コミット・PR・ドキュメント更新・振り返り

- [ ] feature ブランチに最新 master を取り込み push
- [ ] PR 作成 → master へマージ（Merge commit）
- [ ] マージ後に feature ブランチ・worktree を削除
- [ ] 実機でストーリー表示を確認（必要に応じて）
- [ ] 実装後の振り返りを記録（別ファイル `retrospective.md` → モード3）

---

> **振り返りについて**: 実装後の振り返りはこのファイルではなく、同じディレクトリの
> `retrospective.md` に記録する。全タスクが `[x]` になったことを確認してから作成すること。
