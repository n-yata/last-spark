# 振り返り: Codex CLI 用設定一式の削除 (2026-07-04)

## 何をやったか
`.agents/skills/*`(Codex 向けスキル複製)と `.codex/*`(config/hooks/agents)を削除。
b1e1a0e で一括追加された Codex CLI 用の設定一式で、スキルの正本は `.claude/skills/` に
同内容が揃っており、Codex の利用停止に伴い不要になった。

## 学び・申し送り

### ツール引退時は「設定＋参照ドキュメント」をワンセットで棚卸しする
- 本体ツリーに未ステージ削除のまま長期間放置されており、全セッションの `git status` を
  汚し続けていた。ツールをやめると決めた時点で、削除を即 feature ブランチでコミットすべき。
- **残課題**: ルートの `AGENTS.md`(Codex 向け指示書)は今回スコープ外として温存。
  `.agents/` 配下スキルや `.Codex/` カタログへの参照(42行目付近・109〜112行目付近)が
  陳腐化しているため、AGENTS.md 自体を廃止するか参照を修正するかの判断が必要。
